var assert = require('assert');
var pseudoClasses = require('..');

describe('pseudo-classes', function() {

  it('should do something awesome', function() {
    assert.equal(pseudoClasses(), true);
  });
});
